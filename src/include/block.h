#ifndef BLOCK_H
#define BLOCK_H

#include <glm/glm.hpp>

enum class BlockType {
    AIR = 0,
    STONE = 1,
    DIRT = 2,
    GRASS = 3
};

// texture

// directly define texture UV in mesh builder

#endif
